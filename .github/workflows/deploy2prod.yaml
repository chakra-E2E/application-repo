name: depoy to pod
on:
  workflow_dispatch:
    inputs:
      version:
        required: true
        description: "Enter commit sha"
        type: string
concurrency:
  group: roboshop-catalogue-prod
  cancel-in-progress: false
jobs:
  prod:
    if: ${{ github.ref == 'refs/heads/main' }}
    uses: chakra-E2E/Devops-workflows/.github/workflows/prod-workflow.yaml@main
    permissions:  #controls what gittoken can do
      contents: read
      statuses: write
    with:
      version: ${{ inputs.version }}
    secrets: inherit